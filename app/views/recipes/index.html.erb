<% @recipes.each do |recipe| %>
  <h2>Title: <%= link_to recipe.title, "/recipes/#{recipe.id}" %></h2>
  <p>Chef: <%= recipe.chef %></p>
  <p>Ingredients: <%= recipe.ingredients %></p>
  <p>Directions: <%= recipe.directions %></p>
<% end %>

